# กระบวนการทำงานและ QA — ThaiShield AI

> เขียน 2026-08-23 · ครอบคลุมทั้ง `thaishield_ai` (Flutter) และ `thaishield-ai-web-admin` (CMS)
>
> ไฟล์นี้และ QA gate ย้ายเข้ามาอยู่ใน repo เมื่อ 2026-08-23 — ของที่ `CLAUDE.md` บังคับให้ใช้
> ต้องอยู่ใน version control เหมือนสิ่งที่มันตรวจ ส่วน `QA_PHASE_2B.md` และ `INTEGRATION_TEST.md`
> ยังอยู่ที่ workspace root เพราะครอบคลุมทั้งสอง repo

## 1. ปัญหาที่เอกสารนี้แก้

โปรเจกต์นี้มีเช็กลิสต์ QA แบบมือที่ละเอียดมาก (`QA_PHASE_2B.md`, `INTEGRATION_TEST.md`)
แต่ก่อนหน้านี้**ไม่มีอะไรที่รันตัวเองได้บนเครื่องเลย** ผลคือ

- ทุกครั้งที่แก้ฟีเจอร์ ต้องมีคนไล่เช็กลิสต์ทั้งชุดใหม่ ซึ่งกินเวลาจนไม่มีใครทำครบจริง
- เคสที่หลุดบ่อยที่สุดไม่ใช่เคสยาก แต่เป็นเคสที่ **"เคยเทสไปแล้วเมื่อสองสัปดาห์ก่อน"**
- ข้อความบางอย่างพังเงียบ ๆ เช่นตอนเปลี่ยนแพ็กเกจ ข้อความบน Paywall ยังสัญญาว่า
  "กู้คืนการซื้อได้" ทั้งที่ iOS ทำไม่ได้แล้ว — เทสต์ที่มีอยู่ตอนนั้นไม่มีทางจับได้

เอกสารนี้กำหนดว่า **"เสร็จ" แปลว่าอะไร** และให้เครื่องมือที่บังคับใช้ได้จริง

---

## 2. ปิรามิดการทดสอบที่มีตอนนี้

| ชั้น | จำนวน | รันที่ไหน | เวลา | จับอะไร |
|---|---|---|---|---|
| **Unit** — `test/*_test.dart` | 146 | เครื่องพัฒนา | ~3 วิ | ตรรกะ, โมเดล, การคำนวณ, ถ้อยคำ 6 ภาษา |
| **Widget** — `test/paywall_widget_test.dart` | 13 | เครื่องพัฒนา | ~3 วิ | หน้าจอวาดสิ่งที่โมเดลบอกจริงไหม |
| **On-device** — `integration_test/` | 6 journey | emulator / เครื่องจริง | ~2 นาที | บูตจริง, สลับแท็บ, ทดลองใช้ฟรี, เปลี่ยนภาษา |
| **Manual** — `QA_PHASE_2B.md` | ~70 เคส | เครื่องจริง | ~1 ชม. | กล้อง, ไมค์, GPS จริง, deep link, การซื้อ |

**หลักการ:** อะไรที่พิสูจน์ได้โดยไม่ต้องใช้เครื่อง ให้อยู่ชั้นล่างเสมอ ชั้นบนแพงและช้ากว่าหลายเท่า
เทสต์ที่ย้ายลงมาได้แล้วไม่ย้าย คือหนี้ที่ต้องจ่ายทุกรอบที่แก้โค้ด

### คำสั่ง

```bash
export PATH="/c/Users/werapat/flutter/bin:$PATH"
cd /c/Fastwork/thaishield-ai/thaishield_ai

flutter analyze                              # ต้อง No issues found!
flutter test                                 # unit + widget
flutter test integration_test -d emulator-5554   # on-device
```

> ⚠️ `integration_test` **ล้าง SharedPreferences บนเครื่อง** รันบน emulator หรือเครื่องทดสอบเท่านั้น

---

## 3. นิยามของคำว่า "เสร็จ"

งานหนึ่งชิ้นจะเรียกว่าเสร็จได้ ต้องครบทั้ง 5 ข้อ ไม่ใช่แค่ข้อแรก

1. **โค้ด** ทำงานได้ตามที่ตกลง
2. **เทสต์** — มีเทสต์ใหม่ที่จะแดงถ้าฟีเจอร์นี้พังในอนาคต และจำนวนเทสต์รวม**ไม่ลดลง**
3. **`CLAUDE.md` ของทุก repo ที่แตะ** — อัปเดตพร้อมเหตุผล ไม่ใช่แค่บอกว่าทำอะไร
4. **เอกสารลูกค้า** (Rev.4 ใน Google Docs) — สถานะและตัวเลขตรงกับความจริง
5. **QA gate ผ่าน** — ดูข้อ 4

ข้อ 3 กับ 4 คือข้อที่ถูกข้ามบ่อยที่สุด และเป็นข้อที่แพงที่สุดเมื่อข้าม:
งานที่ลูกค้ามองไม่เห็นคืองานที่เก็บเงินไม่ได้ และ `CLAUDE.md` คือบรีฟเดียวที่คนทำงานรอบหน้าจะได้อ่าน

---

## 4. QA gate — รันทุกครั้งที่มีฟีเจอร์ใหม่

```
Workflow: feature-qa
args: { change: "อธิบายสั้น ๆ ว่าแก้อะไร" }
```

ตัว workflow อยู่ที่ `thaishield_ai/.claude/workflows/feature-qa.js` ทำงานแบบนี้

```
                    ┌─ qa-static ─┐
   การเปลี่ยนแปลง ──┼─ qa-device ─┼──▶ ทุก finding ส่งให้ผู้คัดค้าน 2 คน ──▶ รายงานเดียว
                    ├─ qa-copy   ─┤        (ต้องผ่านทั้งคู่ถึงจะรอด)
                    └─ qa-scope  ─┘
```

**ทำไมต้อง 4 มุมที่ไม่คุยกัน** — เพราะแต่ละมุมพังคนละแบบ เทสต์เขียวไม่ได้แปลว่าถ้อยคำถูกกฎหมาย
และถ้อยคำถูกไม่ได้แปลว่าลูกค้าได้รับแจ้ง ถ้าให้ทุกมุมเห็นผลของกันและกัน จะได้ finding ซ้ำ 4 อัน
และเสียอีก 3 อันที่ไม่มีใครมองหา

| Agent | ดูอะไร |
|---|---|
| `qa-static` | analyze + เทสต์ทั้งหมด + สิ่งที่เทสต์เขียวปิดบังไว้ (โค้ดใหม่ที่ไม่มีเทสต์, string key ที่ไม่มีอยู่จริง) |
| `qa-device` | รัน on-device suite, เดินหน้าจอจริงด้วย adb, **อ่าน logcat ทุกครั้ง** และเก็บ screenshot เป็นหลักฐาน |
| `qa-copy` | ถ้อยคำตาม §10 ครบ 6 ภาษา, placeholder ไม่หาย, ไม่สัญญาสิ่งที่แอปทำไม่ได้ |
| `qa-scope` | อยู่ในขอบเขตสัญญาไหม, ชน §7 ไหม, และ **อัปเดตเอกสารครบหรือยัง** |

**ทำไมต้องมีผู้คัดค้าน** — รายงาน QA ที่ผิดแม้ครั้งเดียว จะทำให้รายงานครั้งต่อ ๆ ไปถูกอ่านแบบข้าม ๆ
ทุก finding จึงต้องผ่านคน 2 คนที่มีหน้าที่ล้มมัน: คนหนึ่งตรวจว่าหลักฐานจริงไหม อีกคนตรวจว่า
**ผู้ใช้จริงหรือลูกค้าเดือดร้อนไหม** — ความไม่เรียบร้อยภายในไม่นับเป็น finding

ผลลัพธ์มี 3 ส่วนเสมอ: **confirmed** (ต้องแก้), **refuted** (ถูกล้มพร้อมเหตุผล — ไว้ดูว่า gate เชื่อถือได้แค่ไหน),
และ **untested** (สิ่งที่รอบนี้ไม่ได้ครอบคลุม — เขียนไว้ชัด ๆ เพื่อไม่ให้ "ผ่าน" ถูกตีความเกินจริง)

---

## 4.1 🚨 บทเรียนจากการรัน gate ครั้งแรก — "ผ่าน" ที่ไม่ได้ตรวจอะไรเลย

รอบแรกที่รัน `feature-qa` ผลออกมาว่า **PASS ไม่มี finding เลย** ซึ่งดูดีมาก
ความจริงคือ **reviewer ทั้ง 4 ตัวพังตั้งแต่ยังไม่เริ่ม** — สคริปต์อ้าง agent ผ่าน `agentType`
แต่ registry ยังไม่รู้จักชื่อพวกนั้น `pipeline` แปลง exception เป็น `null`
แล้วโค้ดสรุปผลอ่าน `null` ว่า "ตรวจแล้วไม่เจออะไร"

**นี่คือความล้มเหลวที่อันตรายที่สุดของเครื่องมือ QA** — อันตรายกว่าการไม่มี gate เลย
เพราะถ้าไม่มี gate ทุกคนรู้ตัวว่าต้องระวังเอง แต่ gate ที่โกหกทำให้คนเลิกระวัง

**สิ่งที่แก้ และห้ามแก้กลับ**

1. **ย้าย brief ของ agent มาไว้ในสคริปต์โดยตรง** ไม่พึ่ง registry ที่อาจไม่โหลด
   ไฟล์ใน `thaishield_ai/.claude/agents/` ยังอยู่สำหรับเรียกใช้แบบ interactive — **สองที่นี้ต้องแก้พร้อมกัน**
2. **angle ที่รันไม่สำเร็จ ทำให้ผลรวมเป็น `INCONCLUSIVE` ไม่ใช่ `PASS`** และรายงานชื่อ angle
   ที่ไม่ได้รันออกมาตรง ๆ
3. **ผู้คัดค้านที่ไม่ตอบ ไม่นับเป็นเสียงเห็นด้วย** — finding จะรอดต้องมีคำตอบครบ 2 เสียง

**หลักที่ได้จากเรื่องนี้ ใช้ได้กับทุกระบบอัตโนมัติในโปรเจกต์นี้**

> ทุกครั้งที่เขียนอะไรที่รายงานว่า "ผ่าน" ให้ถามก่อนว่า
> **ถ้าเครื่องมือนี้พังทั้งตัว มันจะรายงานว่าอะไร**
> ถ้าคำตอบคือ "ผ่าน" แปลว่ายังเขียนไม่เสร็จ

หลักเดียวกันใช้ได้กับ monitor ที่ grep หาแต่คำว่าสำเร็จ, health check ที่ timeout แล้วเงียบ,
และเทสต์ที่ pump แล้วไม่ assert อะไรเลย

---

## 5. สิ่งที่ระบบอัตโนมัติทำแทนคนไม่ได้

ต้องเขียนไว้ตรงนี้ ไม่ใช่ปล่อยให้ผลเขียวสื่อความเกินจริง — บน emulator เปล่า สิ่งเหล่านี้
**คือ "ยังไม่ได้ทดสอบ" ไม่ใช่ "ผ่าน"**

- กล้อง (Price Scanner) และไมโครโฟน (SOS / STT)
- GPS จริงและ dialog ขอสิทธิ์ตำแหน่งของระบบ
- Deep link ไปแอป Google Maps — AVD ทั่วไปไม่มีแอปนี้
- การซื้อจริง, การกู้คืน, การตรวจใบเสร็จ
- ฝั่ง iOS ทั้งหมด (ไม่มีเครื่อง Mac — build ผ่าน Codemagic)

รายการเหล่านี้อยู่ใน `QA_PHASE_2B.md` และต้องไล่ด้วยมือก่อนส่งมอบทุกเฟส

---

## 6. ถ้าทีมโตขึ้น

ตอนนี้คนเดียวทำทุกบทบาท เมื่อมีคนเพิ่ม ให้แยกตามนี้ เพราะเป็นเส้นที่ความผิดพลาดเกิดจริง

| บทบาท | รับผิดชอบ | ห้ามเป็นคนเดียวกับ |
|---|---|---|
| **Dev** | โค้ด + unit/widget test | — |
| **QA** | on-device + manual pass + ดูแล `QA_PHASE_2B.md` ให้ทันฟีเจอร์ | คนที่เขียนฟีเจอร์นั้น |
| **Legal/Copy** | §10 ทุกข้อความก่อนขึ้น store | — |
| **Client-facing** | เอกสาร Rev.4, ใบแจ้งหนี้, การยืนยันขอบเขต | — |

เส้นที่สำคัญที่สุดคือ **QA ไม่ควรเป็นคนเขียนฟีเจอร์นั้นเอง** — คนเขียนจะทดสอบเส้นทางที่ตัวเองคิดไว้
ซึ่งเป็นเส้นทางที่ทำงานอยู่แล้วเสมอ

---

## 7. ก่อน merge / ก่อนส่งมอบ

- [ ] `flutter analyze` สะอาด
- [ ] `flutter test` ผ่าน จำนวนไม่ลดลง
- [ ] `flutter test integration_test -d <device>` ผ่าน
- [ ] รัน workflow `feature-qa` แล้ว ไม่มี finding ระดับ critical ค้าง
- [ ] `CLAUDE.md` ของทุก repo ที่แตะ อัปเดตแล้ว
- [ ] เอกสารลูกค้า Rev.4 อัปเดตแล้ว
- [ ] commit ทั้งสอง repo **โดยไม่ต้องถาม**
- [ ] push **เฉพาะเมื่อผู้ว่าจ้างสั่ง** — ห้าม push เอง

> 🚨 **ข้อนี้เปลี่ยนเมื่อ 2026-08-30** เดิมเขียนว่า "ยังไม่ push — ตามข้อตกลงกับ
> ผู้ว่าจ้าง" ซึ่งเป็นข้อตกลงเดิมจริง แต่ผู้ว่าจ้างสั่ง push เองหลายครั้งในวันนั้น
> จนกฎเดิมกลายเป็นเอกสารที่ค้างไม่ตรงกับความเป็นจริง — QA gate จับได้ว่าเป็น
> critical เพราะ push ครั้งนั้นทำให้หน้า `/terms` ขึ้นเว็บสาธารณะอัตโนมัติ
>
> ⚠️ **สิ่งที่ต้องระวังทุกครั้งที่ push repo web admin:** มันคือการ deploy
> เว็บสาธารณะทันทีผ่าน GitHub → App Hosting ไม่ใช่แค่การเก็บโค้ด หน้า
> `/terms` และ `/privacy` เปิดให้คนทั่วไปอ่านได้ทันทีที่ push ผ่าน ดังนั้น
> ข้อความที่มีผลผูกพัน (ราคา · การเรียกเก็บเงิน · การยกเลิก) ต้องให้ผู้ว่าจ้าง
> อ่านก่อน ไม่ใช่หลัง

## Client document — updated 2026-08-30? **No.**

The QA gate flags "no evidence the client doc was updated" as a defect, and it
is right to: a price change the client has not been shown is a change they
have not agreed to.

**Outstanding as of 2026-08-30.** The Rev.4 quotation and status document
(Google Docs) still describes the two one-time passes at $7 and $10, the 3-day
trial leading into a pass, and the iOS restore limitation the client accepted
on 2026-08-23. All three are now wrong. The subscription decision, the $3.50
weekly price and the removal of the iOS limitation have not been written into
it.

The developer cannot edit that document through the Drive integration — it has
to be done through the browser, and a paste into it once wiped the file. So it
is a deliberate, hands-on step, not something to assume happened. Record it
here when it is done, with the date.
