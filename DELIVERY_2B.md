# คู่มือตรวจรับงาน Phase 2B — ThaiShield AI

> ส่งมอบ 27/08/2026 · สำหรับ Android · ใช้เวลาตรวจประมาณ 30–40 นาที
>
> **เปลี่ยนจากรอบ 28/08:** แพ็กเกจเปลี่ยนจาก **บัตรผ่านจ่ายครั้งเดียว** เป็น
> **สมาชิกรายสัปดาห์ $3.50 และรายเดือน $10 แบบต่ออายุอัตโนมัติ** ตามที่ผู้ว่าจ้าง
> แจ้งเมื่อ 30/08 · ยกเลิกได้ตลอดเวลาที่หน้าตั้งค่าของสโตร์ และการกู้คืนการซื้อ
> ใช้ได้ทั้ง Android และ iOS แล้ว ต่างจากรอบก่อนที่ iOS ทำไม่ได้
>
> **เปลี่ยนจากรอบ 24/08:** งาน 2.4 แนะนำเส้นทาง **ตรวจรับได้แล้ว** (ข้อ 6) —
> คีย์เชื่อมต่อบริการคำนวณเส้นทางถูกสร้างเรียบร้อยและทดสอบผ่านครบทั้งสามรูปแบบ
> การเดินทาง ส่วนอื่นของแอปไม่มีการเปลี่ยนแปลง

---

## 1. ไฟล์ที่ส่งมอบ

| ไฟล์ | ใช้ทำอะไร |
|---|---|
| **`thaishield-2B-ตรวจรับ.apk`** | **ใช้ไฟล์นี้ในการตรวจรับ** — มีสวิตช์สำหรับสลับระหว่างสถานะ *ฟรี* กับ *Premium* ได้ทันที ไม่ต้องรอ |
| `thaishield-2B-เวอร์ชันจริง.apk` | เวอร์ชันเดียวกับที่จะขึ้นสโตร์จริง ไม่มีสวิตช์ ใช้ดูว่าผู้ใช้จริงจะเห็นอะไร |

> ⚠️ ทั้งสองไฟล์เป็นแอปตัวเดียวกัน **ติดตั้งทับกัน** — ลงตัวใหม่ตัวเก่าจะถูกแทนที่
> ถ้าอยากสลับไปมา ให้ถอนการติดตั้งก่อนแล้วลงใหม่

**ทำไมต้องมีตัวตรวจรับแยก** — แอปจริงจะให้ผู้ใช้ใหม่ **ทดลองใช้ฟรี 3 วันทันที**
ที่เปิดแอปครั้งแรก แปลว่าถ้าใช้เวอร์ชันจริงตรวจ **จะไม่เห็นหน้าเสนอแพ็กเกจหรือฟีเจอร์ที่ถูกล็อกเลย
ตลอด 3 วันแรก** ซึ่งเป็นส่วนสำคัญของงานที่ต้องตรวจรับพอดี

---

## 2. วิธีติดตั้ง

1. คัดลอกไฟล์ `.apk` ลงเครื่อง Android
2. เปิดไฟล์ → ระบบจะถามว่าอนุญาตให้ติดตั้งจากแหล่งนี้หรือไม่ → กดอนุญาต
3. เปิดแอป → เลือกภาษา → เข้าหน้าหลัก
4. เมื่อแอปขอสิทธิ์เข้าถึงตำแหน่ง **กดอนุญาต** (ไม่อนุญาตจะตรวจแผนที่ไม่ได้)

---

## 3. ตรวจรับงาน 2.5 — หน้าเสนอแพ็กเกจและระบบล็อกฟีเจอร์

### 3.1 หาสวิตช์สำหรับตรวจรับ

ไปที่แท็บ **โปรไฟล์** → เลื่อนลงล่างสุด → จะเห็นกล่องสีเหลืองอ่อนเขียนว่า **"QA: unlock Premium"**

กล่องนี้**มีเฉพาะในไฟล์ตรวจรับ** เท่านั้น ในเวอร์ชันจริงไม่มีและเปิดไม่ได้ด้วยวิธีใด ๆ

### 3.2 สถานะฟรี — ปิดสวิตช์

| ตรวจอะไร | ควรเห็นอะไร | ☐ |
|---|---|---|
| หน้าโปรไฟล์ การ์ดบนสุด | กรอบสีทอง ชวนอัปเกรดเป็น Premium พร้อมปุ่ม "ดูแพ็กเกจ" | ☐ |
| หน้าแผนที่ ปุ่มตัวกรอง (ขวาล่าง) | เป็นรูป **แม่กุญแจ** ไม่ใช่ไอคอนตัวกรอง | ☐ |
| กดปุ่มแม่กุญแจนั้น | เปิดหน้าเสนอแพ็กเกจ พร้อมแถบทองบอกว่าฟีเจอร์นี้ล็อกอยู่ | ☐ |
| แผงรอบตัวคุณ (ลากขึ้นจากขอบล่างของแผนที่) | เห็นรายการอย่างละ **3 รายการ** แล้วปิดท้ายด้วยการ์ดขอบทองบอกว่ามีอีกกี่รายการ | ☐ |
| ตัวเลข 4 ช่องบนแผงนั้น | **ยังแสดงจำนวนเต็ม ไม่ถูกล็อก** — ตั้งใจให้เห็นภาพรวมได้เสมอ | ☐ |
| แตะหมุดร้านบนแผนที่ | ปุ่มดูเส้นทางเป็นรูปแม่กุญแจ | ☐ |
| ของฟรียังใช้ได้ครบ | แผนที่ · หมุดร้าน · พื้นที่แจ้งเตือน · สแกนราคา · SOS · ข่าวหน้าแรก **ทั้งหมดยังใช้ได้ปกติ** | ☐ |

### 3.3 หน้าเสนอแพ็กเกจ

| ตรวจอะไร | ควรเห็นอะไร | ☐ |
|---|---|---|
| จำนวนแพ็กเกจ | **2 แพ็กเกจเท่านั้น** — รายสัปดาห์ $3.50 และรายเดือน $10 | ☐ |
| ต้องไม่มีอะไร | ไม่มีคำว่า *รายปี* *ตลอดชีพ* *จ่ายครั้งเดียว* หรือราคาเป็นเงินบาท | ☐ |
| แตะสลับแพ็กเกจ | วงกลมเลือกย้ายตามที่กด กรอบเขียวย้ายตาม | ☐ |
| ข้อความใต้ปุ่ม | บอกครบ 4 อย่าง — **ต่ออายุอัตโนมัติ** · เก็บเงินผ่านบัญชี Google Play หรือ Apple ID · **ยกเลิกที่หน้าตั้งค่าของสโตร์ ไม่ใช่ในแอป** · ยกเลิกแล้วใช้ต่อได้จนครบรอบที่จ่ายไปแล้ว | ☐ |
| กดปุ่ม "สมัครสมาชิก" | ขึ้นข้อความว่าระบบชำระเงินจะเปิดในเวอร์ชันถัดไป · **ไม่ค้าง ไม่ปลดล็อกอะไร** | ☐ |
| เปลี่ยนภาษาแล้วเปิดใหม่ | ครบทั้ง 6 ภาษา ไม่มีข้อความล้นกรอบ | ☐ |

### 3.4 สถานะ Premium — เปิดสวิตช์

| ตรวจอะไร | ควรเห็นอะไร | ☐ |
|---|---|---|
| ปุ่มตัวกรองบนแผนที่ | เปลี่ยนจากแม่กุญแจเป็นไอคอนตัวกรอง กดแล้วเปิดแผงตัวกรองได้ | ☐ |
| แผงรอบตัวคุณ | เห็นครบทุกรายการ **ไม่มีการ์ดขอบทองปิดท้าย** | ☐ |
| แตะหมุดร้าน | ปุ่มดูเส้นทางใช้งานได้ — ตรวจละเอียดที่ข้อ 6 | ☐ |
| หน้าโปรไฟล์ | การ์ดบนสุดเปลี่ยนเป็นสีเขียว บอกว่า Premium กำลังใช้งาน | ☐ |

---

## 4. ตรวจรับระบบทดลองใช้ฟรี 3 วัน

ต้องเริ่มจากสถานะเครื่องใหม่ **ล้างข้อมูลแอปก่อน**: ตั้งค่า → แอป → ThaiShield AI → พื้นที่จัดเก็บ → ล้างข้อมูล

| ตรวจอะไร | ควรเห็นอะไร | ☐ |
|---|---|---|
| เปิดแอปครั้งแรกหลังล้างข้อมูล → ไปโปรไฟล์ | การ์ดบนสุดเขียนว่า **"กำลังทดลองใช้ฟรี · เหลือ 3 วัน"** | ☐ |
| ลองใช้ฟีเจอร์ที่เคยล็อก | ใช้ได้ทั้งหมดเหมือน Premium | ☐ |
| ปิดแอปแล้วเปิดใหม่ | ยังเป็นสถานะทดลองใช้ · **จำนวนวันไม่รีเซ็ตกลับเป็น 3** | ☐ |
| ไม่ต้องกรอกบัตรใด ๆ | ถูกต้อง — ไม่มีการขอข้อมูลการชำระเงินและไม่มีการตัดเงิน | ☐ |

---

## 5. ตรวจรับงานเพิ่มเติม — แผงรอบตัวคุณบนแผนที่

| ตรวจอะไร | ควรเห็นอะไร | ☐ |
|---|---|---|
| เปิดแท็บแผนที่ | แผนที่เลื่อนมาที่ตำแหน่งของคุณเอง มี **จุดสีน้ำเงิน** และ **วงรัศมี 1 กม.** | ☐ |
| ดูแผงด้านล่างโดยไม่ต้องลาก | เห็นหัวข้อ "รอบตัวคุณในระยะ 1 กม." และ **ตัวเลข 4 ช่องครบ** | ☐ |
| ลากแผงขึ้น | เห็นรายการแจ้งเตือนใกล้เคียงและพันธมิตร **พร้อมระยะทางเป็นเมตร** | ☐ |
| แตะรายการใดก็ได้ | แผนที่เลื่อนไปยังจุดนั้นและเปิดรายละเอียดให้ | ☐ |
| ปุ่มลอยมุมขวาล่าง | ตอนแผงย่ออยู่กดได้ทุกปุ่ม · ตอนลากแผงขึ้นปุ่มจะจางหายไปเพื่อไม่บังข้อมูล | ☐ |

---

## 6. ตรวจรับงาน 2.4 — แนะนำเส้นทาง

> ⚠️ **เปิดสวิตช์ Premium ก่อน** (โปรไฟล์ → QA: unlock Premium) ไม่งั้นจะเจอหน้าเสนอแพ็กเกจแทน
> เพราะฟีเจอร์นี้เป็นของ Premium

เริ่มจาก **แท็บแผนที่ → แตะหมุดร้าน → กดปุ่มสีเขียว "ดูเส้นทาง"**

| ตรวจอะไร | ควรเห็นอะไร | ☐ |
|---|---|---|
| หน้าที่เปิดขึ้นมา | เต็มจอ หัวสีเขียวเข้ม บรรทัดบนเขียนว่า "ไปยัง <ชื่อร้าน>" | ☐ |
| ระหว่างรอ | ขึ้น "กำลังระบุตำแหน่งของคุณ…" แล้วเปลี่ยนเป็น "กำลังคำนวณเส้นทาง…" | ☐ |
| เส้นทางที่วาด | **เส้นเขียวหนาวิ่งตามถนนจริง** ไม่ใช่เส้นตรงลากทะลุตึก · หมุดเขียวที่ตัวเรา หมุดส้มที่ปลายทาง | ☐ |
| การจัดมุมกล้อง | เห็นเส้นทางทั้งเส้นพอดีจอ ปลายเส้นไม่โดนตัดขอบ | ☐ |
| การ์ดสรุปด้านล่าง | ชื่อปลายทาง + **เวลาโดยประมาณ** + **ระยะทาง** + ปุ่มเปิด Google Maps | ☐ |
| สลับเป็น **เดิน** | ตัวเลขเปลี่ยน · เวลาเดิน**นานกว่ารถอย่างเห็นได้ชัด** · รูปเส้นทางเปลี่ยน | ☐ |
| สลับเป็น **ขนส่งสาธารณะ** | ได้เส้นทาง **หรือ** ขึ้นว่าไม่พบเส้นทางรูปแบบนี้ — **ทั้งสองแบบถือว่าผ่าน** ขึ้นกับว่าย่านนั้นมีบริการไหม | ☐ |
| กดกลับไป **รถยนต์** ภายใน 5 นาที | **ตอบทันที ไม่มีวงหมุน** | ☐ |
| กดปุ่มสลับโหมดรัว ๆ ระหว่างกำลังคำนวณ | ปุ่มกดไม่ติดจนกว่าจะคำนวณเสร็จ | ☐ |
| กด **"เปิดใน Google Maps"** | เด้งเข้า**แอป** Google Maps (ไม่ใช่เบราว์เซอร์) · ต้นทางปลายทางและรูปแบบการเดินทาง**ตรงกับที่ดูไว้** | ☐ |
| กดย้อนกลับ | กลับหน้าแผนที่ตำแหน่งเดิม ไม่มีหน้าค้าง | ☐ |
| ข้อความกำกับท้ายจอ | เห็นตลอดเวลา ว่าเวลาและระยะทางเป็นค่าประมาณ และให้ปฏิบัติตามกฎจราจร | ☐ |
| ปิดสิทธิ์ตำแหน่งในตั้งค่า แล้วเปิดหน้านี้ใหม่ | ขึ้นข้อความขอสิทธิ์ตำแหน่ง **พร้อมปุ่มลองใหม่** | ☐ |
| เปิดโหมดเครื่องบิน แล้วกดลองใหม่ | ขึ้นว่าเชื่อมต่ออินเทอร์เน็ตไม่สำเร็จ + ปุ่มลองใหม่ · **แอปไม่ปิดตัวเอง** | ☐ |

**ข้อที่ขอให้ดูเป็นพิเศษคือข้อ "กดกลับไปรถยนต์ภายใน 5 นาที"** — ที่ต้องตอบทันที
เพราะระบบจำคำตอบเดิมไว้ 5 นาที บริการนี้ของ Google **คิดเงินเป็นรายครั้งที่เรียก**
การจำคำตอบไว้จึงเป็นสิ่งที่กันไม่ให้ค่าใช้จ่ายบานปลายตอนเปิดใช้งานจริง
ถ้าข้อนี้มีวงหมุนขึ้นมา รบกวนแจ้งทันที

## 7. สิ่งที่ต้องทดสอบบนเครื่องจริงเท่านั้น

ผู้พัฒนาทดสอบบนเครื่องจำลองแล้ว แต่สิ่งเหล่านี้เครื่องจำลองทดสอบแทนไม่ได้ **รบกวนช่วยดูให้ด้วย**

| ฟีเจอร์ | วิธีทดสอบ | ☐ |
|---|---|---|
| **สแกนราคา** | แท็บสแกน → ถ่ายรูปเมนูหรือป้ายราคาจริง → ดูว่าอ่านราคาได้และแสดงช่วงราคาทั่วไป | ☐ |
| **SOS ด้วยเสียง** | แท็บ SOS → กดค้างแล้วพูด → ดูว่าแปลเป็นภาษาไทยและอ่านออกเสียงได้ | ☐ |
| **ตำแหน่ง GPS จริง** | เดินออกไปที่อื่นแล้วกดปุ่มระบุตำแหน่ง → จุดสีน้ำเงินและตัวเลขรอบตัวต้องเปลี่ยนตาม | ☐ |
| **โทรเบอร์ฉุกเฉิน** | โปรไฟล์ → ศูนย์ช่วยเหลือฉุกเฉิน → กดโทร (กดยกเลิกก่อนต่อสายได้) | ☐ |

---

## 8. พบปัญหาแจ้งอย่างไร

ถ่ายภาพหน้าจอ พร้อมบอก 3 อย่าง

1. **กดอะไรก่อนหน้านั้น** (ลำดับการกด)
2. **คาดว่าจะเห็นอะไร** และ **เห็นอะไรจริง**
3. **รุ่นเครื่องและเวอร์ชัน Android**

ยิ่งบอกลำดับการกดละเอียด ยิ่งแก้ได้เร็ว เพราะผู้พัฒนาสามารถทำซ้ำตามได้ทันที

---

## 9. สรุปสิ่งที่ส่งมอบในรอบนี้

**งานตามสัญญา**

- ✅ งาน 2.5 หน้าเสนอแพ็กเกจ + ระบบล็อกฟีเจอร์ — พร้อมตรวจรับ
- ✅ งาน 2.4 แนะนำเส้นทาง — **พร้อมตรวจรับแล้วในรอบนี้** (ข้อ 6)

**งานเพิ่มเติมที่ไม่คิดค่าใช้จ่าย**

- ✅ ย้ายข้อมูลรอบตัวคุณเข้ามาไว้ในหน้าแผนที่
- ✅ ระบบทดลองใช้ฟรี 3 วัน
- ✅ ปรับแพ็กเกจใหม่ 2 แบบ พร้อมระบบบันทึกสิทธิ์การซื้อ
- ✅ หน้านโยบายความเป็นส่วนตัว
- ✅ ระบบตรวจสอบคุณภาพอัตโนมัติ 165 ชุด
